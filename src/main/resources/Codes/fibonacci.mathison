num fiboRec ( num n ) {
    num numero
    si ( n <= 1 ) //CASO BASE
	{
        numero = 1
    }
    sino          //CASO TRIVIAL
	{
        num enesimo = fiboRec ( n - 1 ) + fiboRec ( n - 2 )
	    numero = enesimo
	}
	imprimir(numero)
	devolver numero
}